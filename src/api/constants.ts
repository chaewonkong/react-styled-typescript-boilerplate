export enum API_ENDPOINTS {
  MOCK = '/v1/mock',
}

export enum FETCH_STATUS_CODE {
  DEFAULT = -1,
  LOADING = 99,
  OK = 200,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
  NOT_FOUND = 404,
  UNKNOWN = 500,
}
